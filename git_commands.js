// git init => initialize git repo
// git status => checks status
// git add  . => add file to git to track
// git commit -m "<message></message>" =>to commit code on git
// to see remote => git remote -v
//  to add remote => git remote add <name> <link>
// git branch -M main
//  git push -u <name> <branch>

//git branch -m master main - rename master to main . -m means move i.e it renames
// git remote set-url  origin https://github.com/pokhrelyadav/amazon.git    to add our github repo remote link to cloned projects

//? To change recent commit message
// git commit --amend      - opens text editor , edit and press Esc to enter command mode and write  :wq or :q! and enter then,
// git push --force
